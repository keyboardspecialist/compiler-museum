{
  "name": "compiler-museum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "./build.sh",
    "serve": "cd site && python3 -m http.server 8000",
    "test": "node test/test-museum.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
